<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hoshimusubi.mapper.MemberMapper">

    <select id="getMemberById" resultType="com.hoshimusubi.domain.MemberVO">
        SELECT MEMBER_ID, USERNAME, PASSWORD, EMAIL, NICKNAME, GENDER, BIRTHDATE, PROFILE_IMAGE_PATH
        FROM MEMBERS
        WHERE MEMBER_ID = #{memberId}
    </select>

    <update id="updateProfileImagePath">
        UPDATE MEMBERS
        SET PROFILE_IMAGE_PATH = #{profileImagePath}
        WHERE MEMBER_ID = #{memberId}
    </update>

</mapper>